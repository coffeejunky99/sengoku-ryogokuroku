import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coffeejunky99.sengokuryogokuroku',
  appName: '戦国領国録',
  webDir: 'dist',
};

export default config;
